spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8444

eureka:
  client:
    service-url:
      defaultZone: http://100.30.182.141:8761/eureka/
