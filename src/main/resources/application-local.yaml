spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_services
    username: postgres
    password: 1212
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8989

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
